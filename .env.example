# Copy this file to .env and fill in your values.
SUBREDDIT_NAME=ColorPuzzleGame

# Where scraper/auth.py saves (and fetch_post.py / extract_board.py read)
# the authenticated Reddit session. Run `python -m scraper.auth` to create it.
REDDIT_STORAGE_STATE_PATH=reddit_state.json
