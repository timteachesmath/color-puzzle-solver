{
  "name": "color-puzzle-solver",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node --test src/solver.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
